import type { ProjectStatus } from '../../types/projects.type';

export const PROJECT_STATUS_LABELS: Record<ProjectStatus, string> = {
  development: 'Em desenvolvimento',
  completed: 'Concluído',
};
